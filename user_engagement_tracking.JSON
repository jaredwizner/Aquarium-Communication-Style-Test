{
  "user_engagement_tracking": {
    "user_id": {
      "description": "A unique identifier for each user taking the Aquarium Communication Style Test.",
      "type": "string"
    },
    "engagement_metrics": {
      "total_time_spent": {
        "description": "The total amount of time the user has spent on the test so far, tracked in seconds.",
        "type": "integer"
      },
      "average_time_per_question": {
        "description": "The average time the user takes to answer each question, in seconds.",
        "type": "float"
      },
      "completion_rate": {
        "description": "The percentage of questions answered compared to the total number of questions in the test.",
        "type": "float",
        "format": "percentage"
      },
      "session_count": {
        "description": "The total number of sessions the user has spent interacting with the test. Useful for determining if a user has broken the test into multiple sittings.",
        "type": "integer"
      },
      "session_breaks": {
        "description": "The list of timestamps representing when the user paused the test or closed their session.",
        "type": "array",
        "items": {
          "timestamp": {
            "type": "string",
            "format": "date-time",
            "description": "The time when the user paused the session."
          }
        }
      }
    },
    "engagement_quality": {
      "response_consistency": {
        "description": "Tracks the consistency of user's responses to determine how engaged they are during the test. This metric considers factors like hesitation, changes in response, or rapid answering.",
        "type": "string",
        "enum": ["High", "Medium", "Low"]
      },
      "focus_score": {
        "description": "A calculated score representing the user’s focus level throughout the test, derived from average time per question and frequency of switching answers.",
        "type": "integer",
        "min": 0,
        "max": 10
      }
    },
    "timestamp_last_engagement": {
      "description": "Indicates the last time the user interacted with the test, useful for tracking ongoing engagement.",
      "type": "string",
      "format": "date-time"
    }
  }
}
